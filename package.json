{
  "name": "restaurant-review-platform",
  "version": "2.0.0",
  "description": "Restaurant Review Platform — IFQ636 Assignment 2 (full-stack, design patterns, CI/CD)",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "test": "npm test --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "author": "IFQ636 Team",
  "license": "ISC"
}
